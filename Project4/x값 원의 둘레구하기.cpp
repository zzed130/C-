#include <iostream>
#include <math.h>
#include <cmath>



using namespace std;


int main() {
	int X, Y, Z;
	int a, b;

	cin >> a >> b;

	X = sqrt(a) + sqrt(a);
	Y = a * 3.14159;
	Z = b * 3.14159;

	cout << X + Y + Z;

	system("pause");
	//이거 나중에 루트 2a 가 구글에서 어떤건지 알아보기
	/*pow(a,b) : a의 b승
	sqrt(x) : x의 제곱근*/






}